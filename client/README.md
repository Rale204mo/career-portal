Client (React) Setup & Deploy
1. cd client
2. npm install
3. For local dev: npm start
4. To build: npm run build
5. To deploy on Firebase Hosting: ensure firebase CLI logged in and configured, then firebase deploy (set hosting to client/build)